#include "PewPew.h"

/*
PewPew::PewPew(ofVec2f pos, ofVec2f dir)
{
	image.loadImage("bullet.png");
	Reset(pos, dir);
}

void PewPew::Reset(ofVec2f pos, ofVec2f dir)
{
	m_position.set(pos);
	m_direction.set(dir);
	m_size = 10.0f;
	m_BulletSpeed = 3.0f;
}

void PewPew::Draw()
{
	image.draw(m_position, m_size, m_size);
}

void PewPew::Update()
{
	m_position += m_direction * m_speed;
	m_speed = m_direction.normalize();
	if (m_destination.distance(m_position) > 5)
		m_position = m_position + m_speed * m_minion_speed;
}


PewPew::~PewPew()
{
}
*/